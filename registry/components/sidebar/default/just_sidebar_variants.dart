/// Visual variants for [JustSidebar].
enum JustSidebarVariant {
  /// Default standard sidebar aligned flush to the screen container
  default_,

  /// Floating panel offset from screen boundaries with margin and shadow
  floating,

  /// Inset sidebar layout with distinct margins and capsule shape items
  inset,
}
