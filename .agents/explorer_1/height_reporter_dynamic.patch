diff --git a/apps/showcase/lib/height_reporter.dart b/apps/showcase/lib/height_reporter.dart
index 68425d0..e3a47b1 100644
--- a/apps/showcase/lib/height_reporter.dart
+++ b/apps/showcase/lib/height_reporter.dart
@@ -18,8 +18,10 @@ class _HeightReporterState extends State<HeightReporter> {
 
   void _reportHeight() {
     if (!mounted) return;
-    const double height = 180.0;
-    if (height == _lastReportedHeight) return;
+    final renderBox = _key.currentContext?.findRenderObject() as RenderBox?;
+    if (renderBox == null) return;
+    final height = renderBox.size.height;
+    if (height == _lastReportedHeight || height <= 0) return;
 
     _lastReportedHeight = height;
     web.window.parent?.postMessage(
