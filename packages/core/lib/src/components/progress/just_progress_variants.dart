export '../../theme/preset_tokens.dart' show JustProgressSize;

/// The visual style variants for [JustProgress].
enum JustProgressVariant {
  /// Horizontal linear progress bar.
  linear,

  /// Circular progress indicator.
  circular,
}
