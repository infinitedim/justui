/// The visual style variants for [JustTable].
enum JustTableVariant {
  /// Bordered table where all cells have borders.
  default_,

  /// Alternating row backgrounds, no vertical cell borders.
  striped,

  /// Minimal design with only horizontal separators, no vertical borders.
  minimal,
}
