name: just_ui_tokens
description: Design tokens for JustUI — colors, spacing, typography, radius, shadows, and durations.
version: 0.7.7
publish_to: none

environment:
  sdk: ">=3.13.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
